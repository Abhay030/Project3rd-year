import React from "react"

const Front = () => {
    return (
        <div>
            <h1>Welcome to the Front Page</h1>
            <p>This is a React component rendered using JSX.</p>
        </div>
    );
};

export default Front;
